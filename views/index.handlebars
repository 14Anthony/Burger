<h1>HAMBURGERLER</h1>

<h2>EatABurger</h2>
{{!-- !-- from the cats formatting with a button addition. bootstrapp ....????  I think I may have to add a libray to the main????ask mike --}}


<ul>
    {{#each burger}}
    {{#unless devoured}}
    {{ burger/burger-block devoured=false}}
    <li>{{burgerName}}<button class="btnD" type="button" data-id={{id}}>EAT AWAY</button></li>
    {{/unless}}
    {{/each}}
</ul>

<ul>
    {{#each burger}}
    {{#if devoured}}
    {{burger/burger-block devoured = true}}
    <li>
        {{burgerName}}
    </li>
    {{/if}}
    {{/each}}data-id
</ul>


<h2>GIVE to the BURGERLER </h2>

<form class="create-form">

    <div class="form-group">
        <label for="ca">BURGER NAME:</label>
        <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">

        <input type="radio" name="sleepy" value="1" checked> READY<br>
        <input type="radio" name="sleepy" value="0"> DEVOUR
    </div>

    <button type="submit">BRING IT</button>
</form>