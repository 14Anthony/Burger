<h1>HAMBURGERLER</h1>
<div>
    <img src="/public/assets/hamburglar-png-16-transparent.png">
</div>
<h1>EatABurger!</h1>
<form class="create-form">

    <div class="form-group">
        <input type="text" id="ca" name="name" placeholder="BurgerName">
    </div>
    <button type="submit">BRING IT</button>
</form>
{{!-- !-- from the cats formatting with a button addition. bootstrapp ....????  I think I may have to add a libray to the main????ask mike --}}

<h2>HUNGRY?</h2>

<ul>
    {{#each burger}}
    {{#unless devoured}}
    {{ burger/burger-block devoured=false}}
    <li>{{burgerName}}<button class="btnD" type="button" data-id={{id}}>EAT AWAY</button></li>
    {{/unless}}
    {{/each}}
</ul>

<h2>Devoured </h2>
<ul>
    {{#each burger}}
    {{#if devoured}}
    {{burger/burger-block devoured = true}}
    <li>{{burger_name}}<button class="btn" type="button" data-id="{{this.id}}">Done!</button></li>
    {{/if}}
    {{/each}}data-id
</ul>