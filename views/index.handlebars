<h1>HAMBURGERLER</h1>
<div>
    <img src="/public/assets/hamburglar-png-16-transparent.png">
</div>
<h1>EatABurger!</h1>
<form class="create-form">

    <div class="form-group">
        <label for="ca">BURGER NAME:</label>
        <input type="text" id="ca" name="name">
    </div>
    <button class="btn" type="submit">BRING IT</button>
</form>
{{!-- !-- from the cats formatting with a button addition. bootstrapp ....????  I think I may have to add a libray to the main????ask mike --}}

<h2>HUNGRY?</h2>

<ul>
    {{#each burger}}
    {{#unless devoured}}
    {{ burger/burger-block devoured=false}}
    <li>{{burgerName}}<button class="btnD" type="button" data-id={{id}}>EAT AWAY</button></li>
    {{/unless}}
    {{/each}}
</ul>

<h2>Devoured </h2>
<ul>
    {{#each burger}}
    {{#if devoured}}
    {{burger/burger-block devoured = true}}
    <li>
        {{burgerName}}
    </li>
    {{/if}}
    {{/each}}data-id
</ul>